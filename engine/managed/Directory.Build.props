<Project>
  <PropertyGroup>
    <PublishDir>$(SolutionDir)out\</PublishDir>
  </PropertyGroup>

	<ItemGroup>
		<Compile Include="..\..\..\swig-gen\**\*.cs"/>
	</ItemGroup>

	<PropertyGroup>
		<DebugType>portable</DebugType>
	</PropertyGroup>
</Project>