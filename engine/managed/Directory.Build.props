<Project>
  <PropertyGroup>
    <PublishDir>$(SolutionDir)out\</PublishDir>
	<Platforms>AnyCPU;x64;x86;ARM64</Platforms>
  </PropertyGroup>

	<ItemGroup>
		<Compile Include="..\..\..\swig-gen\**\*.cs"/>
	</ItemGroup>

	<PropertyGroup>
		<DebugType>portable</DebugType>
	</PropertyGroup>
</Project>